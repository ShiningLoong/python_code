# coding: utf8


def merge(listA, listB):
    lst = list()
    keyA,keyB,key = 0,0,0
    while True:
        if keyA < len(lstA):
            
        if listA[keyA] < listB[keyB]:
            lst[key] = listA[keyA]
            


def merge_sort(lst):
    pass
